# 我把 WorkBuddy 的 440 个专家全部「拆穿」了

> 一份关于"专家市场到底卖什么"的拆解报告。长文 + 自查网站 + GitHub 仓库，一次性放出。
> 声明：以下内容全部通过与 WorkBuddy 里的 AI 助理连续对话完成——让它读本机清单、逆向客户端逻辑、写下载脚本、做统计分析。没有用任何特权或未授权手段。

---

## 方法：就是和 WorkBuddy 助理聊天

先交代清楚这个拆解是怎么做的，避免大家误以为我有内部渠道：

**我和 WorkBuddy 助理的对话，一步步让它替我干活：**

1. 让它去翻本机的 `manifest.json` 和 `expert_center.json`，搞清专家清单的字段结构
2. 让它去读 WorkBuddy 客户端代码，找到「点击召唤」背后到底发生了什么
3. 让它写脚本把公开 COS 源里的 421 个专家包批量下载解压
4. 让它扫描全部插件包，统计技能、构件、重复引用
5. 再让它把结果整理成网页、长文、自查工具

整个过程就是**用 WorkBuddy 拆解 WorkBuddy**。助理能看到本机文件、能执行命令、能读客户端代码，剩下的就是提问和交叉验证。这也是我写这篇文章想表达的一个观点：**你手里这个助手，本身就是个逆向工具。**

---

## 一、先讲清楚三层结构

打开 WorkBuddy 任何一个专家，**你看到的标签和它真正的行为之间，至少隔着两层**：

```
专家(展示层清单)  manifest.json
   ↓ 解析
插件包(可下载单元)  bundles/<plugin>.tar.gz
   ↓ 解压
构件(agents/skills/rules/scripts/mcp/hooks)
```

关键结论：

- **标签**只是清单里的展示元数据，不影响执行
- **真正决定行为**的是插件包里的 `agents/<name>.md`（人格提示词）+ 私有 skill
- 同名 skill 被不同专家调用时，因为外层 prompt 不同，产出形态可能天差地别

---

## 二、48 秒，装完 421 个市场专家

440 个专家里有 **421 个走公开 COS 源**（`acc-1258344699.cos.accelerate.myqcloud.com/.../bundles/<name>.tar.gz`），剩下 19 位没有公开安装包（走运营平台签名 URL），其中只有 15 位是真正的腾讯内部专属。

**但"没有公开包"不等于"外部不能用"** —— manifest 里有 `visibility` 字段：19 位里 12 位是 `internal_only`，真正外部可召唤的只有 **4 位**：AICoding 架构专家团、庄老师（企鹅教师助手）、送报报、MigraQ。剩下 15 位（贝安、阿拉丁、CPQ、哈勃、觅诊、腾讯招聘、小T、问图、甄需明等）才是**腾讯内部专属**，外部账号无论如何搜索都不会出现。

助理写了个 Python 脚本，并发 10 路下载全部 421 个 tar.gz，**48 秒搞定，0 失败**。

执行完得到一个让人沉默的目录：

```
plugins/marketplaces/experts/plugins/
├── 421 个插件目录
├── 16485 个文件
├── 1572.3 MB
└── 712 份 agent 提示词
```

---

## 三、召唤 = 真安装（很多人没意识到）

很多用户以为「点击召唤」只是把专家从云端加到一个列表里，**其实**：

1. 触发 `ExpertPluginService.ensureDownloaded()`
2. 下载 tar.gz
3. 解压到 `plugins/<name>/`
4. 增量回写 `marketplace.json`
5. 留 `.downloaded_at` 时间戳

也就是说，**召唤一次就真实落盘一次**。召唤 421 次 = 421 次网络下载 + 解压 + 写盘。这也是手工一个个点会卡住的根本原因。

**绕过方式**：助理在客户端代码里发现一个 `explicitBundleUrl || 默认COS地址` 的双源降级设计，默认地址就是上面那个公开桶，无需签名。批量脚本只是复刻了客户端的下载解压流程，但走了原始降级路径。

完整逆向过程在仓库 `拆解报告/06-安装机制逆向.md`，含 5 步日志取证、3 条安装通道对比、19 位无公开包专家可见性全表、7 条工程设计借鉴。

---

## 四、609 个技能，真正的「专家肌肉」

把 421 个插件包拆开，里面共有 **801 个私有 skill 目录**，去重后是 **609 个不同名**的 skill。再加上 664 个全局 skill 池里的 skill，总量 **1465**。

> 此前的报告说「1165 个」是按目录数（含没有 `SKILL.md` 的中间层）+ 混算两个池的结果，已被修正。

**最被引用的 10 个 skill**：

| 排名 | Skill | 引用专家数 | 定位 |
|---|---|---:|---|
| 1 | `minimax-docx` | 15 | 生态级文档基建 |
| 2 | `market-researcher` | 12 | 通用调研 |
| 3 | `neodata-financial-search` | 12 | 金融数据检索 |
| 4 | `anti-distill` | 10 | 内容反蒸馏 |
| 5 | `browser-use` | 9 | 浏览器自动化 |
| 6 | `xlsx-author` | 8 | 表格生成 |
| 7 | `humanizer` | 7 | 文本人味化 |
| 8 | `westock` | 7 | A股行情 |
| 9 | `audit-xls` | 6 | 底稿审计 |
| 10 | `deep-research` | 6 | 深度研究 |

**一个反直觉的发现**：87%（531 个）的 skill 是**单专家独占**的，只有 78 个被 ≥2 个专家复用。这说明大部分专家是「带着自己的专精工具」来的，而不是共用一个公共库。

> 那些「标签」相同的专家，他们共享的不是代码，而是**外层 prompt**。

---

## 五、同标签 ≠ 同能力（L0–L4 差异模型）

我们对比了 5 组同标签专家的实际构件（标签、提示词、rules、skill、编排），得到一个四层差异模型：

| 层次 | 影响 | 典型差异 |
|---|---|---|
| L0 标签 | 不影响执行 | 标签字符串完全相同 |
| L1 人格 agent.md | **最主要** | 同名 skill + 完全不同的指令链 |
| L2 rules | 强约束 | 输出格式、强制条款差异 |
| L3 skills/mcp/scripts | 能力边界 | 调用的工具集不同 |
| L4 members 团队编排 | 协作模式 | 团队型专家的成员分工 |

**一个具体例子**：`minimax-docx` 这个 skill 在 15 个专家里都出现，文件名相同。但装载它的「ContentCreator」和「ComplianceAuditor」，外层 prompt 把它当作不同的工具使用——一个输出品牌故事，一个输出合规条款。

**执行差异的四种机制**：
1. 外层 prompt 决定了 skill 输出的取舍
2. rules 文件是硬性条款，越多越「霸总」
3. skill 的 `description` 字段被模型当路由表读，写得越具体，越不容易误用
4. 团队型专家靠 members 分工，单体专家靠单 prompt 强

---

## 六、几个有意思的「之最」

- **最重**：VietnamFinanceTaxExpert 626MB（内嵌越南财税数据库）
- **文件最多**：JiayiAdsAnalyticsExpert 962 文件
- **私有 skill 最多**：investment-banking 22 个、fin-research-expert 18 个、equity-research 16 个
- **最被引用的 skill**：minimax-docx（15 个专家）
- **同名的坑**：两个「腾讯云知（乐享）」，一个是知识问答（市场公开专家），一个是演示文稿设计（内部专属）

---

## 七、自查网站：现在你也能查

我做了一个**独立自查网站**：

🔗 **https://augusta-waveryu.github.io/workbuddy-expert-ecosystem/**

打开后：

1. 找到你本机文件 `~/.workbuddy/plugins/marketplaces/experts/.codebuddy-plugin/marketplace.json`
2. 点击「选择 marketplace.json」上传
3. **1 秒内算出**：你装了多少 / 覆盖率 / 缺哪些分类 / 你持有的技能包数

**所有解析都在浏览器本地完成，不会上传任何数据**。也可以直接粘贴插件名（每行一个），或者点「加载示例」看我装齐的状态。网站还内置了 19 位无公开包专家的 `visibility` 可见性标记——哪 4 位外部能召唤、哪 15 位是内部专属，一清二楚。

如果你也想**批量装 421 个专家**，下面这条命令就够（无需登录、不占积分）：

```bash
curl -o /tmp/expert_center.json \
  https://acc-1258344699.cos.accelerate.myqcloud.com/workbuddy/expert-marketplace/expert_center.json
```

---

## 八、GitHub 仓库：所有内容都公开

我把全部拆解、代码、报告、自查网站源码都开源了：

📦 **https://github.com/Augusta-waveryu/workbuddy-expert-ecosystem**

包含：

- `公众号长文.md`（本篇）
- `拆解报告/00–07` 共 8 份 Markdown（连接器清单 / 全局概览 / 专家构件映射 / Skill 拆解 / 关键实现学习 / 差异分析 / 安装机制逆向 / 排行榜）
- `docs/index.html` + `docs/data.js`（自查网站源码）
- 一份网页版速查报告（8 章，含 288 个 h3 锚点的可折叠目录树）

欢迎 star / fork / issue。如果你是 WorkBuddy 重度用户，强烈建议**先自查再决定要不要召唤**——你会发现自己真正缺的，往往不是某位专家，而是一个被 12 个专家同时引用的 `market-researcher` skill。

---

## 九、几点反思

- **公开 COS 源这件事本身值得点赞**：愿意让数据可枚举，是生态自信的表现。但代价是所有专家提示词可被匿名下载，签名通道的防护形同虚设。
- **19 位无公开包专家里 15 位是内部专属、外部召唤不了** 是个产品问题：manifest 里躺着 `internal_only` 的专家，外部用户在 GUI 里看不到也搜不到，却要忍受"440 个专家"的营销数字——可见列表 ≠ 可召唤列表，而且没有提前说明。
- **「点击召唤」的真实安装成本**应该被 UI 暴露出来，否则用户会以为这是云端调用，结果发现本机硬盘涨了 1.5GB。
- **「标签 = 能力」是错觉**。同样的「数据分析」标签下，可能藏着 5 种完全不同的 prompt 范式。自查网站至少能让你看清这点。
- **最诚实的一点**：这篇文章是 WorkBuddy 助理帮我写的，包括逆向、脚本、统计和排版。我做的事是提问、验证、纠错（比如它最初把 skill 数统计成 1165，我们重算才修正为 1465）。**AI 干的活可以很多，但结论要人来核。**

---

> 本文与所有数据基于 **2026-08-19** 当天本地扫描。如果 WorkBuddy 后续更新了专家市场，欢迎提 issue 让我跟进。
